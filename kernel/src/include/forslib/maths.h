#ifndef __INCLUDE_FORSLIB_MATHS_H__
#define __INCLUDE_FORSLIB_MATHS_H__

#define MAX(a, b) ((a) >= (b) ? (a) : (b))
#define MIN(a, b) ((a) <= (b) ? (a) : (b))

#endif /* __INCLUDE_FORSLIB_MATHS_H__ */